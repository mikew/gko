News:
    actAs:
        Timestampable:
        Sluggable:
            fields: [title]
            alias: key
            indexName: news_slug
    columns:
        title: varchar(255)
        body: text