Authors:
    actAs:
        Timestampable:
        Sluggable:
            fields: [handle]
            alias: key
            indexName: authors_slug
    columns:
        name: string
        handle: string
        password:
            type: string
            # confirm: true