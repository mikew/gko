Posts:
    actAs:
        Timestampable:
        Sluggable:
            fields: [title]
            alias: key
            indexName: posts_slug
    columns:
        title: varchar(255)
        body: text
        body_markdown: text
