Posts:
    actAs:
        Timestampable:
        Sluggable:
            fields: [title]
            alias: key
            indexName: posts_slug
    columns:
        title: string
        body: text
        body_markdown: text
        authors_id: integer
